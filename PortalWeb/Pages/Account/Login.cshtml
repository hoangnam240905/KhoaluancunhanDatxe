@page
@model PortalWeb.Pages.Account.LoginModel
@{
    ViewData["Title"] = "Dang nhap";
    Layout = "_LayoutAuth";
}

<div class="auth-card">
    <div class="auth-card-header">
        <div class="auth-logo">🚗</div>
        <h1 class="h4 fw-bold mb-1">Car Rental System</h1>
        <p class="text-muted small mb-0">Dang nhap theo vai tro cua ban</p>
    </div>
    <div class="auth-card-body">
        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert alert-danger py-2">@Model.ErrorMessage</div>
        }
        <form method="post">
            <div class="mb-3">
                <label asp-for="Input.Email" class="form-label">Email</label>
                <input asp-for="Input.Email" class="form-control" placeholder="email@example.com" autocomplete="username" />
            </div>
            <div class="mb-3">
                <label asp-for="Input.Password" class="form-label">Mat khau</label>
                <div class="input-group">
                    <input asp-for="Input.Password" type="password" class="form-control" placeholder="••••••••" autocomplete="current-password" />
                    <button type="button" class="btn btn-toggle-password" aria-label="Hien mat khau">
                        <span class="toggle-label">Hien</span>
                    </button>
                </div>
            </div>
            <button type="submit" class="btn btn-primary w-100 mt-2">Dang nhap</button>
        </form>
        <div class="demo-box mt-4">
            <strong>Demo:</strong><br />
            Admin: <code>admin@carrental.vn</code><br />
            Dieu phoi: <code>dispatcher@carrental.vn</code><br />
            Khach hang: <code>customer1@gmail.com</code><br />
            Mat khau: <code>Password123!</code>
        </div>
        <p class="text-center mt-3 mb-0 small">
            Chua co tai khoan? <a asp-page="/Account/Register" class="fw-semibold">Dang ky ngay</a>
        </p>
    </div>
</div>
